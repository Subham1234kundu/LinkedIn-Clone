import React from 'react'
const INITIAL_STATE = {
    user:null,
}
const UserReducer = (state = INITIAL_STATE,action) => {
  switch(action.type){
    default:
        return state;
  }
}

export default UserReducer
